namespace SignalForge.Domain;

/// <summary>Known rule type identifiers. Phase 2 supports only <see cref="SignalTypeEquals"/>.</summary>
public static class RuleTypes
{
    public const string SignalTypeEquals = "SignalTypeEquals";
}
